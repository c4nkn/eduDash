@page
@model IndexModel
@{
    ViewData["Title"] = "Home";
}

<div class="text-center mt-5">
    <h1>Redirecting to login page...</h1>
    <p>Didn't redirect? <a asp-page="/Login">Click</a> to login.</p>
</div>
